//Hence, we are not provide the name of the function so that it's called anonymous function 

const fullname = function (fName, lName) {
    return `${fName} ${lName}`;
}

console.log(fullname("Eftekher", "Shuvo"));